name: snapshot-phase3i-materializer

on:
  push:
    branches: ["agent/phase3c-hardening-runner"]
    paths:
      - ".github/workflows/snapshot-phase3i-materializer.yml"

permissions:
  actions: read
  contents: write

jobs:
  snapshot:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/phase3c-hardening-runner
      - name: Snapshot Phase 3I materializer runs
        shell: bash
        run: |
          set -euo pipefail
          runs=$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs?branch=agent/phase-3i-state-lineage&per_page=100")
          mkdir -p .status
          jq '{captured_at:(now|todate),runs:[.workflow_runs[] | select(.name == "phase3i-materialize") | {id,status,conclusion,head_sha,html_url,created_at,updated_at,run_attempt}]}' \
            <<<"$runs" > .status/phase3i-materializer-runs.json
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .status/phase3i-materializer-runs.json
          git commit -m "snapshot Phase 3I materializer runs" || true
          git push origin HEAD:agent/phase3c-hardening-runner
