name: phase3e-format-final

# Trigger after workflow registration.
on:
  push:
    branches: ["agent/phase-3e-longitudinal-events"]
    paths:
      - ".github/workflows/phase3e-format-final.yml"

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/phase-3e-longitudinal-events
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install development dependencies
        run: python -m pip install -e '.[dev,ml]'
      - name: Format and validate Phase 3E
        run: |
          ruff format src/hybrid_trader/phase3e.py scripts/assess_phase3e_longitudinal.py tests/test_phase3e_longitudinal.py
          ruff check --fix src/hybrid_trader/phase3e.py scripts/assess_phase3e_longitudinal.py tests/test_phase3e_longitudinal.py
          mypy --python-version 3.12 src
          pytest tests/test_phase3e_semantic_budget.py tests/test_phase3e_longitudinal.py -q
          pytest -m 'not optional_ml' -q
          pytest -m optional_ml -q
          python -m pip check
      - name: Remove one-time formatter and commit
        shell: bash
        run: |
          set -euo pipefail
          rm -f .github/workflows/phase3e-format-final.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "format and validate Phase 3E longitudinal collection"
          git push origin HEAD:agent/phase-3e-longitudinal-events
