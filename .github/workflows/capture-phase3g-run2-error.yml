name: capture-phase3g-run2-error

on:
  push:
    branches: ["agent/phase3c-hardening-runner"]
    paths:
      - ".github/workflows/capture-phase3g-run2-error.yml"

permissions:
  actions: read
  contents: write

jobs:
  capture:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/phase3c-hardening-runner
      - name: Capture corrected Phase 3G failure
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .status
          gh run view 29640785583 --repo "$GITHUB_REPOSITORY" --log-failed > /tmp/phase3g-run2.log
          {
            grep -nE 'Would reformat|error:|Error|FAILED|AssertionError|Traceback|Found [0-9]+ error|E       ' /tmp/phase3g-run2.log || true
            echo '=== tail ==='
            tail -n 220 /tmp/phase3g-run2.log
          } > .status/phase3g-run2-error.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .status/phase3g-run2-error.txt
          git commit -m "capture corrected Phase 3G failure" || true
          git push origin HEAD:agent/phase3c-hardening-runner
